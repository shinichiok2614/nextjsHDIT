//extension
auto close tag
auto complete tag
auto rename tagcode spelll checker

npx create-docusaurus@latest my-website classic
cd my-website
npm start
xoá upgrading.md
nextjs->app router

npx create-next-app@latest
>no tailwind css
>yes src/ directory

cd nextjs-lean
yarn next dev

// mở bằng vscode
code .

component nào render ở server
component nào render ở client

file-system based routing

bắt buộc phải đặt tên là page.tsx (giống như index)

href trong next/link k phai load lại
href trong link thì phải load lại

'use client' //client chỉ render 1 phần thôi
useRouter()

root layout
nested layout

next > 13.4.12
react-bootstrap > 2.8.0
bootstrap > 5.3.0

layout > import 'bootstrap/dist/css/bootstrap.min.css';
nếu không chạy được css thì xoá .next

dùng react bootstrap thì phải thêm 'use client'
'use client': convert to Client component

yarn add swr
useSWR()

